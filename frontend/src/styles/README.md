# Styles DnD VTT

La source CSS active est `frontend/src/styles/index.css`.

`index.css` importe les modules CSS dans l'ordre de cascade voulu.

Ne pas importer directement l'ancien fichier monolithique `frontend/src/styles.css`.
S'il existe encore, il doit rester archivé dans `_archive/`.
